<jobs>
    <job xmlns:a="b2tplxml" a:bindlist="$jobs" group="@group" name="@name" uri="#{@group/@name}">
        <triggers uri="#{@group/@name/triggers}"/>
        <log uri="#{@group/@name/log}"/>
    </job>
</jobs>